{% extends 'layoutDiscussion.html.twig' %}

{% block pageTitle %}Tricks{% endblock %}

{% block content %}
    <h1>Tricks</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trick.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ trick.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ trick.description }}</td>
            </tr>
            <tr>
                <th>TrickGroup</th>
                <td>{{ trick.trickGroup }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tricks_index') }}">back to list</a>

    <a href="{{ path('tricks_edit', {'id': trick.id}) }}">edit</a>

    {{ include('tricks/_delete_form.html.twig') }}

    <a href="{{ path('comments_new', {'id': trick.id}) }}">Commenter</a>

{% endblock %}
